{
  "kind": "collectionType",
  "collectionName": "encoders",
  "info": {
    "name": "encoder",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "applicant": {
      "via": "encoders",
      "model": "applicant"
    }
  }
}
